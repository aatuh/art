[package]
name = "artwork-black-cube"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "The Black Cube / White Room installation for the Art gallery."
license = "UNLICENSED"
publish = false

[dependencies]
gallery-core = { path = "../gallery-core" }

[lints.rust]
unsafe_code = "forbid"
